services:
  license:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
    volumes:
      - license-data:/app/data
    restart: unless-stopped

volumes:
  license-data:
